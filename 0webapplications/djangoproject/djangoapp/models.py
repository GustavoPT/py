
# Create your models here.
from django.db import models

class Person(models.Model):
    first_name = models.CharField(max_length=30)
    last_name = models.CharField(max_length=30)

class card:

class manager:
	
}
class Book:
     id;
     title;
     author;
     isbn;
     imagePath;
    
	public Book(Long id, String title, String author, String isbn, String imagePath) {
		super();
		this.id = id;
		this.title = title;
		this.author = author;
		this.isbn = isbn;
		this.imagePath = imagePath;
	}
    
}

class Employee:

class Product:

class User: